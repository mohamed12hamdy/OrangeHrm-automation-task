# OrangeHrm-automation-task
Built an automation framework for OrangeHRM using Selenium WebDriver and TestNG with Page Object Model (POM). Covered login scenarios (positive/negative) using data-driven testing with JSON. Implemented Log4j logging, explicit waits, and reusable utilities to ensure stability, scalability, and maintainability.
